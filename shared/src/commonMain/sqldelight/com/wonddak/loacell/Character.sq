CREATE TABLE Character (
  name TEXT NOT NULL PRIMARY KEY,
  username TEXT NOT NULL,
  server TEXT NOT NULL,
  class TEXT NOT NULL,
  level TEXT NOT NULL,
  FOREIGN KEY(username) REFERENCES UserInfo(name) ON DELETE CASCADE
);


insertCharacterInfo:
INSERT INTO Character VALUES (?,?,?,?,?);

selectByUserName:
SELECT * FROM Character WHERE username = ?;