# LoaCell 작업 지침

이 문서는 이 저장소에서 수행하는 모든 Codex/Agent 세션의 공통 규칙이다. 하위 디렉터리에 더 구체적인 `AGENTS.md`가 있으면 그 지침을 우선한다.

## 작업 시작과 범위

- 작업 전 `design.md`와 관련 모듈의 구조·의존성을 확인한다. UI 작업은 디자인 문서의 테마, 간격, 접근성, CTA 규칙을 따른다.
- 요청 범위와 완료 기준을 먼저 정한다. 불확실하거나 범위를 크게 바꾸는 가정이 필요하면 구현 전에 질문한다.
- 요청하지 않은 리팩터링, 기능, 의존성, 예외 처리 정책을 추가하지 않는다.
- 기존 변경 사항은 사용자의 작업으로 간주한다. 요청과 무관한 파일을 수정·되돌리거나 포맷하지 않는다.
- 변경 후에는 변경 범위에 맞는 가장 작은 검증을 수행하고, 실행하지 못한 검증은 이유와 함께 보고한다.

## 아키텍처와 모듈 경계

- 클린 아키텍처를 지킨다. 도메인 모델과 repository 인터페이스는 `core:model`에 두고, 구체 데이터 접근 구현은 `data:*` 또는 해당 data 계층에 둔다.
- UI는 `SharedUI`, 플랫폼 진입점과 플랫폼 전용 조립은 `androidApp` 및 각 플랫폼 source set에 둔다. 공통 코드를 우선하고 플랫폼 API는 `expect/actual` 또는 플랫폼 전용 파일로 격리한다.
- 의존성은 바깥 계층에서 안쪽 계층으로만 향하게 한다. 도메인 계층은 Compose, Android/iOS, Firebase 등의 구현 세부 사항을 알지 않아야 한다.
- 모듈의 책임에 맞는 위치에 코드를 추가한다. 편의를 위해 UI·도메인·데이터 코드를 한 모듈이나 파일에 섞지 않는다.
- 새 의존성이나 모듈 추가는 기존 경계로 해결할 수 없는 경우에만 한다.

## 상태와 UI

- 단방향 데이터 흐름을 유지한다: 사용자 이벤트는 ViewModel/상태 소유자에 전달하고, 상태는 불변 UI 상태로 UI에 내려보내며, UI는 상태를 직접 변경하지 않는다.
- Compose 화면은 가능한 한 상태를 받아 렌더링하는 함수로 유지한다. 네트워크·저장소 호출과 비즈니스 규칙은 Composable에 넣지 않는다.
- 상태, 이벤트, 일회성 효과는 화면 또는 기능 단위로 명확히 분리하고, 서로 다른 기능의 상태를 공유 전역 상태에 무분별하게 추가하지 않는다.
- UI 변경은 `design.md`를 따른다: Material 3 파란 팔레트와 시스템 라이트/다크 모드, 4dp 간격(`LoaCellSpace`), 16dp 카드 모서리, 최소 48dp 터치 영역, 접근 가능한 content description을 유지한다.

## 코드 구성

- 파일 하나에 서로 다른 기능을 과도하게 넣지 않는다. 화면, 상태, 이벤트, 데이터 변환, 재사용 UI는 역할과 기능 단위로 패키지·파일을 나눈다.
- 반대로 단순한 변경을 위해 불필요한 추상화나 지나친 파일 분할을 만들지 않는다. 기존 패키지 관례와 가장 가까운 위치를 따른다.
- 이름은 책임과 데이터 흐름이 드러나게 짓고, 중복 로직은 같은 기능 경계 안에서만 추출한다.
- 공개 API와 모델 변경은 영향받는 호출부·플랫폼 구현을 함께 확인한다.

## 품질과 보안

- 새 동작이나 수정한 로직에는 현실적인 범위의 테스트를 추가하거나 기존 테스트를 보완한다. UI는 중요한 상태(로딩, 비어 있음, 오류, 완료)를 확인한다.
- 토큰, 키, 개인정보, `google-services.json`, keystore 및 기타 비밀 정보를 출력·커밋·하드코딩하지 않는다.
- Kotlin/Gradle 명령을 포함한 셸 명령은 `rtk`를 접두사로 사용한다.
- 빌드 생성물과 로컬 설정 파일은 추적하지 않으며, 의도하지 않은 대규모 변경이 보이면 멈추고 확인한다.
